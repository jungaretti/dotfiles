[theme]
file = "native"

[icons]
name = "awesome5"

[[block]]
block = "cpu"
interval = 1
format = "{utilization}"

[[block]]
block = "memory"
interval = 1
clickable = false
format_mem = "{mem_used_percents}"

[[block]]
block = "time"
interval = 1
locale = "en_US"
format = "%Y-%m-%d %I:%M:%S %p"
